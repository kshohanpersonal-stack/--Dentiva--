import {createServer} from 'node:http';import {readFile} from 'node:fs/promises';import {extname,join,normalize} from 'node:path';import {fileURLToPath} from 'node:url';const root=join(fileURLToPath(new URL('.',import.meta.url)),'..');const types={'.html':'text/html','.js':'text/javascript','.css':'text/css','.svg':'image/svg+xml'};createServer(async(req,res)=>{try{let p=normalize(join(root,req.url==='/'?'index.html':req.url));if(!p.startsWith(root))throw 403;res.setHeader('Content-Type',types[extname(p)]||'text/plain');res.end(await readFile(p))}catch(e){res.statusCode=404;res.end('Not found')}}).listen(process.env.PORT||4173,'0.0.0.0',()=>console.log('Dentiva preview on '+(process.env.PORT||4173)));