import {cp,rm,mkdir,writeFile} from 'node:fs/promises';import {join} from 'node:path';await rm('dist',{recursive:true,force:true});await mkdir('dist/Dentiva-Portable-v1.0.0',{recursive:true});for(const f of ['index.html','styles.css','package.json'])await cp(f,join('dist/Dentiva-Portable-v1.0.0',f));await cp('src',join('dist/Dentiva-Portable-v1.0.0/src'),{recursive:true});await cp('assets',join('dist/Dentiva-Portable-v1.0.0/assets'),{recursive:true});await writeFile('dist/README.txt','Dentiva v1.0.0\nThis preview package runs with Node.js 20+: node scripts/serve.mjs\nA native Windows installer requires a Windows build host.\n');console.log('Prepared dist/Dentiva-Portable-v1.0.0 source package.');