import {cp,rm,mkdir,writeFile} from 'node:fs/promises';import {join} from 'node:path';const out='dist/source-preview/Dentiva-UI-Preview-v1.0.0';await rm('dist/source-preview/Dentiva-UI-Preview-v1.0.0',{recursive:true,force:true});await mkdir(out,{recursive:true});for(const f of ['index.html','styles.css','package.json'])await cp(f,join(out,f));await cp('src',join(out,'src'),{recursive:true});await cp('assets',join(out,'assets'),{recursive:true});await writeFile('dist/source-preview/README.txt','This is a UI/source preview only. It is not a production Windows release.\n');console.log('Prepared dist/source-preview.');