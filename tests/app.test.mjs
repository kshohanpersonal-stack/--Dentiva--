import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';test('product has no seeded patient data',async()=>{const source=await readFile('src/app.js','utf8');assert.match(source,/No patients registered/);assert.doesNotMatch(source,/John Doe|Sample Patient|Lorem ipsum/)});test('brand assets exist',async()=>{const svg=await readFile('assets/dentiva.svg','utf8');assert.match(svg,/DENTIVA|viewBox/)});